---
---

<section class="hero-gradient relative pt-20">
    <div class="absolute inset-0 noise pointer-events-none"></div>

    <div class="max-w-6xl mx-auto px-4 sm:px-6 py-12 sm:py-20 pb-16 sm:pb-32 relative z-10">
        <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
            <!-- Left Column -->
            <div class="fade-in">
                <div class="inline-flex items-center gap-2 bg-white/10 border border-white/20 rounded-full px-4 py-1.5 mb-6">
                    <span class="w-2 h-2 bg-emerald-400 rounded-full animate-pulse"></span>
                    <span class="text-slate-300 text-sm">Built for volunteer-driven nonprofits</span>
                </div>

                <h1 class="hero-headline font-display text-4xl sm:text-5xl lg:text-6xl font-semibold text-white leading-[1.1] mb-6">
                    Never miss another volunteer or donor inquiry again.
                </h1>

                <p class="text-lg sm:text-xl text-slate-300 leading-relaxed mb-8 sm:mb-6 max-w-lg">
                    MyRecruiter answers every supporter question across your website, email, and social — guiding people to donate or volunteer the moment interest sparks, even when your team is off the clock.
                </p>

                <!-- Proof Bar -->
                <div class="flex flex-col gap-1 mb-10 text-sm">
                    <span class="text-emerald-400 font-semibold">&#10003; 65.6% of conversations turn into action</span>
                    <span class="text-emerald-400 font-semibold">&#10003; 8× more volunteer applications</span>
                    <span class="text-emerald-400 font-semibold">&#10003; 49.4% of engagement happens after hours</span>
                </div>

                <div class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4 mb-3 w-full sm:w-auto">
                    <a href="demo" class="w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-emerald-500 hover:bg-emerald-400 text-white px-5 sm:px-7 py-3 sm:py-3.5 rounded-full font-semibold text-sm sm:text-base transition-all shadow-lg shadow-emerald-500/25">
                        Get Your Personalized Demo
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                        </svg>
                    </a>
                    <a href="#lean-teams" class="w-full sm:w-auto inline-flex items-center justify-center gap-2 text-white px-5 sm:px-7 py-3 sm:py-3.5 rounded-full font-medium text-sm sm:text-base border border-white/20 hover:bg-white/5 transition-all">
                        Learn How It Works
                    </a>
                </div>

                <p class="text-slate-300 text-center lg:text-left text-sm">
                    Live in 48 hours · We handle everything · No technical skills needed
                </p>
            </div>

            <!-- Right Column - Widget Screenshot -->
            <div class="fade-in flex justify-center lg:justify-end mt-8 lg:mt-0" style="transition-delay: 200ms">
                <div class="relative">
                    <div class="widget-shadow rounded-2xl overflow-hidden float-animation">
                        <video
                            src="/videos/chat-demo.mp4"
                            autoplay
                            loop
                            muted
                            playsinline
                            class="w-56 sm:w-72 h-auto"
                        ></video>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="absolute bottom-0 left-0 right-0 h-8" style="box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15)"></div>
</section>
