---
const logos = [
    { src: "/images/national-angels-logo.svg", alt: "National Angels" },
    { src: "/images/atlanta-angels-logo.svg", alt: "Atlanta Angels" },
    { src: "/images/austin-angels-logo.svg", alt: "Austin Angels" },
    { src: "/images/FV Logo 753x802.svg", alt: "Foster Village" },
    { src: "/images/bbbs-logo.svg", alt: "Big Brothers Big Sisters of Central Texas" },
];
---

<section class="pt-6 pb-10 bg-white border-b border-slate-200">
    <div class="max-w-5xl mx-auto px-6">
        <p class="text-center text-xs text-slate-400 mb-8 font-medium tracking-widest uppercase">
            Trusted by nonprofits who rely on volunteers to deliver their mission
        </p>
        <div class="flex flex-wrap justify-between items-center gap-8 md:gap-6 max-w-4xl mx-auto">
            {logos.map((logo) => (
                <div class="logo-item">
                    <img
                        src={logo.src}
                        alt={logo.alt}
                        loading="lazy"
                    />
                </div>
            ))}
        </div>
        <p class="text-center text-sm text-slate-500 mt-8">
            Supporting thousands of volunteer and donor conversations every month.
        </p>
    </div>
</section>

<style>
    .logo-item {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .logo-item img {
        height: 3.5rem;
        width: auto;
        max-width: 160px;
        object-fit: contain;
        filter: grayscale(100%);
        opacity: 0.5;
        transition: all 0.3s ease;
    }

    @media (min-width: 768px) {
        .logo-item img {
            height: 4rem;
            max-width: 180px;
        }
    }

    .logo-item:hover img {
        filter: grayscale(0%);
        opacity: 1;
    }

    /* Make Foster Village logo darker to match others */
    .logo-item:nth-child(4) img {
        opacity: 0.7;
    }

    .logo-item:nth-child(4):hover img {
        opacity: 1;
    }

    /* Optical centering for BBBS horizontal logo */
    .logo-item:last-child img {
        transform: translateY(-2px);
    }
</style>
